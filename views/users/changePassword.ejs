<%- include ../layouts/cabeza.ejs %> 
<link rel="stylesheet" href="/stylesheets/components.css">
<link rel="stylesheet" href="/stylesheets/form.css">
<%- include ../layouts/cabecera.ejs %> 

<%- include ../layouts/menu-lateral.ejs%> 

<%- include ../layouts/messages.ejs%>

<main class="page-content" id="page-content">

  <div class="page-content-container">

    <header>
      <h1>Cambiar contraseña</h1> 
    </header>

    <section class="col-md-6">
      <form method="post" action="/users/changePassword?codigo=<%= user[0].codigo %>" class="form" id="formulario">
        <div class="field" id="group-oldpassword">
          <label class="field-label" for="password">Contraseña anterior:</label>
          <input class="field-input" type="password" name="oldPassword" required>
        </div>
        <div class="field" id="group-password">
          <label class="field-label" for="re-password">Contraseña nueva:</label>
          <input class="field-input" type="password" id="password" name="password" required>
          <span class="icon" id="icon">
          </span>
          <p class="input-message"></p>
        </div>
        <div class="field" id="group-confirm_password">
          <label class="field-label" for="re-password">Repita la nueva contraseña:</label>
          <input class="field-input" type="password" id="confirm_password" name="confirm_password" required>
          <span class="icon" id="icon">
          </span>
          <p class="input-message">Las contraseñas no son iguales.</p>
        </div>
        <div class="buttons-container">
          <a href="/users/myUserProfile?codigo=<%= user[0].codigo %> " class="button-form" type="button">Cancelar</a>
          <button class="button-form button-primary" type="submit">Guardar</button>
        </div>
      </form>
    </section>

    <div class="note-box col-md-10">
      <p>Las contraseñas deben cumplir con los siguientes requisitos:</p>
      <ul>
        <li>Deben tener por lo menos 8 carácteres.</li>
        <li>Deben tener al menos una minúscula, una mayúscula, un número y un caracter especial (@#$%!).</li>
        <li>No deben contener caracteres repetidos tales como 1111, 2222, AAAA o CCCC.</li>
      </ul>
    </div>
  </div>

</main>
<script src="/javascripts/changePasswordValidation.js"></script>
<script src="/javascripts/message.js"></script>
<%- include ../layouts/pie.ejs%> 
